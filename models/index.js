const User = require(`./User`);
const Category = require(`./Category`);
const Post = require(`./Post`);
const Notifications = require(`./Notifications`);

module.exports = { User, Category, Post, Notifications };
